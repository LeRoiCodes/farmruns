import React from "react";

const NotReady = () => {
  return (
    <div className="w-full h-full flex justify-center items-center">
      <p className="font-oswald">Sorry this section is not ready yet!</p>
    </div>
  );
};

export default NotReady;
